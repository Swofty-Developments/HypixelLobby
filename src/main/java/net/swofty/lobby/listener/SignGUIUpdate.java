package net.swofty.lobby.listener;

import net.swofty.lobby.util.signgui.MinecraftReflector;
import net.swofty.lobby.util.signgui.SignGUIUpdateEvent;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;

public class SignGUIUpdate implements Listener {
    @EventHandler
    public void onUpdate(SignGUIUpdateEvent event) {
    }
}